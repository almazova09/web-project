community:
  enabled: true

monitoringPasscode: "supersecretpass123"

sonarProperties:
  sonar.web.host: "0.0.0.0"
  sonar.web.context: "/"

service:
  type: LoadBalancer
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-health-probe-protocol: "Http"
    service.beta.kubernetes.io/azure-load-balancer-health-probe-path: "/api/system/status"

